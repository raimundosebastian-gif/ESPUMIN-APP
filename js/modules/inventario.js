import { fetchData, log } from "../core/api.js";

document.addEventListener("DOMContentLoaded", async () => {
  log("Módulo Inventario cargado.");

  // Ejemplo de carga de datos
  // const productos = await fetchData("/api/productos");
});
